<%= form_for(@shiasurprise) do |f| %>
  <% if @shiasurprise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shiasurprise.errors.count, "error") %> prohibited this shiasurprise from being saved:</h2>

      <ul>
      <% @shiasurprise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
